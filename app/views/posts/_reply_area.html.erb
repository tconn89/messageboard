    <%= form_for :reply, :url => "/posts/#{post.id}/reply/create" do |f| %>
      <%= f.text_area :message, :required => true, :class => 'message_input'%>
      <%= f.submit :class => 'btn btn-primary' %>
    <% end %>